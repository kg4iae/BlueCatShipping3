# MS SQL Database Configuration (Required in .env or server environment)
MSSQL_SERVER="SQL5109.site4now.net"
MSSQL_DATABASE="db_a7ef02_bluecatbobbins"
MSSQL_USER="db_a7ef02_bluecatbobbins_admin"
MSSQL_PASSWORD="YourDbPasswordHere"
MSSQL_PORT="1433"
MSSQL_ENCRYPT="false"

# APP_ENV: "dev" (maps to [dbo].[shippingdev]) or "prod" (maps to [dbo].[Shipping])
APP_ENV="dev"

# APP_PASSWORD: Protection password for web app login (default: shipstation123)
APP_PASSWORD="shipstation123"

# Note: EasyPost API keys (Test & Production) and EasyPost Mode are stored directly
# in your MS SQL Database table [dbo].[Configuration] and managed from Settings -> EasyPost.
# No .env variables required for EasyPost!

